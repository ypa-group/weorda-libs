[project]
name = "weorda-events"
version = "0.1.0"
description = "Domain-event contract per event-bus-contract: DomainEvent, envelope codec, bus, dispatcher. Stdlib only."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/weorda_events"]
